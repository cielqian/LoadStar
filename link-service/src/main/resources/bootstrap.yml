spring:
  application:
    name: link-service
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: loadstar.top:8848
        shared-dataids: redis.yml,loadstar.yml
        refreshable-dataids: redis.yml,loadstar.yml
        file-extension: yml
server:
  port: 9083