spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: loadstar.top:8848
        shared-dataids: redis.yml,kafka.yml,loadstar.yml,nacos.yml,zipkin.yml
        refreshable-dataids: redis.yml,kafka.yml,loadstar.yml,nacos.yml,zipkin.yml
        file-extension: yml
server:
  port: 9080