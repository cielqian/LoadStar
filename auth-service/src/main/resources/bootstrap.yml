spring:
  application:
    name: auth-service
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: loadstar.top:8848
        shared-dataids: redis.yml,kafka.yml,loadstar.yml,nacos.yml
        refreshable-dataids: redis.yml,kafka.yml,loadstar.yml,nacos.yml
        file-extension: yml
server:
  port: 9081